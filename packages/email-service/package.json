{
  "name": "@agentpass/email-service",
  "version": "0.1.0",
  "type": "module",
  "description": "Email service for agent mailboxes via Cloudflare Email Workers",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentpass/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260210.0",
    "@types/node": "^25.2.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
