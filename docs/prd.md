# AgentPass — The Identity Layer for Autonomous AI Agents

## Проблема

AI агенти (Claude Code, OpenClaw, CrewAI, AutoGPT, тощо) стають дедалі автономнішими, але вони **не можуть існувати в інтернеті як самостійні сутності**:

- Агент не може зареєструватись на сайті — у нього немає email
- Агент не може пройти SMS-верифікацію — у нього немає телефону
- Агент не може підтвердити свою особу — у нього немає identity
- Агент не може повторно залогінитись — він не пам'ятає свої credentials
- Сервіси не можуть відрізнити легітимного агента від спам-бота

Результат: щоразу коли агент натикається на auth flow, він зупиняється і чекає на людину. Автономність зламана.

**Authentication — визнаний bottleneck #1 в AI agent ecosystem.**

---

## Рішення

**AgentPass** — це паспортна система для AI агентів. Кожен агент отримує криптографічно підписану цифрову ідентичність (Agent Passport), яка дозволяє йому автентифікуватись на будь-якому сервісі в інтернеті.

AgentPass працює у двох режимах:
1. **Native** — сервіс інтегрував AgentPass SDK → агент авторизується миттєво, як "Login with Google"
2. **Fallback** — сервіс не інтегрований → агент реєструється як людина, використовуючи інфраструктуру свого паспорта (email, phone, browser)

---

## Ключові сутності

### Agent Passport

Центральна сутність системи. Це криптографічний документ, що ідентифікує агента.

**Кожен агент = окремий паспорт.** Агенти не шарять credentials між собою, навіть якщо належать одному owner-у. Це забезпечує ізоляцію: компрометація одного агента не впливає на інших.

```json
{
  "passport_id": "ap_7xk2m9f3",
  "version": "1.0",
  
  "identity": {
    "name": "KDN Sales Bot",
    "description": "Autonomous sales agent for kdn.agency",
    "public_key": "ed25519:MCowBQYDK2VwAyEA...",
    "created_at": "2026-02-11T10:00:00Z"
  },

  "owner": {
    "id": "owner_r4k2m",
    "email": "roman@kdn.agency",
    "verified": true
  },

  "capabilities": {
    "email": {
      "address": "kdn-sales-bot@agent-mail.xyz",
      "can_send": true,
      "can_receive": true
    },
    "phone": {
      "number": "+1-555-0142",
      "sms_only": true
    },
    "browser": {
      "enabled": true,
      "max_sessions": 3
    }
  },

  "trust": {
    "score": 87,
    "level": "verified",
    "factors": {
      "owner_verified": true,
      "email_verified": true,
      "age_days": 30,
      "successful_auths": 156,
      "abuse_reports": 0
    }
  },

  "credentials_vault": {
    "services_count": 12,
    "encrypted": true,
    "encryption": "AES-256-GCM"
  },

  "permissions": {
    "max_registrations_per_day": 10,
    "allowed_domains": ["*"],
    "blocked_domains": ["banking.*", "gov.*"],
    "requires_owner_approval": ["payments", "delete_account"],
    "auto_approved": ["registration", "login", "read_data"]
  },

  "audit": {
    "total_actions": 1243,
    "last_action": "2026-02-11T09:45:00Z",
    "log_retention_days": 90
  }
}
```

### Owner (Власник)

Реальна людина або організація, яка створює і контролює агентів:

- Верифікується через email або OAuth (Google/GitHub)
- Може мати кілька агентів (кожен зі своїм passport)
- Несе відповідальність за дії своїх агентів
- Бачить повний audit log
- Може в будь-який момент заблокувати/відкликати агента
- Налаштовує permissions (що агент може робити без запиту)
- Отримує webhook-нотифікації про дії агентів
- Вирішує CAPTCHA і інші проблеми, з якими агент не впорався сам

### Service (Сервіс)

Будь-який вебсайт або API, з яким агент хоче взаємодіяти:

- **Integrated** — встановив AgentPass SDK, підтримує native auth
- **Non-integrated** — звичайний сайт, агент використовує fallback

---

## Архітектура зберігання і безпеки

### Принцип: Private key ніколи не покидає машину агента

```
Машина агента (локально):
├── private_key (Ed25519) — НІКОЛИ нікуди не передається
├── credential_vault.db (SQLite, зашифрований AES-256-GCM)
│   ├── github.com → { username, password, cookies }
│   ├── linear.app → { username, password, cookies }
│   └── ... (всі credentials агента)
└── passport.json (локальна копія паспорта)

AgentPass API (сервер):
├── public_key (Ed25519) — для verify
├── owner info — хто створив агента
├── trust score — репутація
├── metadata (name, capabilities, created_at)
├── audit log — історія дій
└── passport status (active / revoked)
```

### Чому так

- **Навіть якщо зламають AgentPass API** — зловмисник отримає тільки public keys. Підробити підпис агента без private key неможливо. Credentials не витечуть, бо vault зашифрований і живе локально.
- **Якщо агент втратив машину** — private key втрачений, credentials не відновити. Треба створити новий passport. Це безпечніше ніж ризик компрометації.
- **AgentPass API впав** — native auth тимчасово не працює, але fallback (browser mode) працює далі, бо credentials локально.

### Web3 (Future Roadmap)

Поточна архітектура готова до переходу на on-chain identity:
- Public key + metadata можуть жити в смарт-контракті (Base/Ethereum)
- Verify стає повністю децентралізованим — будь-хто може перевірити без центрального API
- Це вирішує проблему "а що якщо AgentPass зникне"
- Для MVP — не реалізуємо, але проектуємо архітектуру так, щоб перехід був можливий

---

## Механіка роботи

### 1. Створення агента (Onboarding)

```
Owner (Roman) → AgentPass Dashboard або CLI:

1. Реєструється як owner (email verification)
2. Створює нового агента:
   - name: "KDN Sales Bot"
   - description: "Handles lead outreach"
   - capabilities: [email, browser]
   - permissions: { auto_approved: ["registration", "login"] }
3. AgentPass генерує:
   - Пару ключів Ed25519
   - Private key → зберігається ТІЛЬКИ локально на машині агента
   - Public key → реєструється на AgentPass API
   - Email: kdn-sales-bot@agent-mail.xyz
   - Passport ID: ap_7xk2m9f3
4. Owner отримує MCP config для підключення до Claude Code
5. Owner налаштовує webhook URL для нотифікацій (Telegram, Slack, email)
```

Після цього агент може використовувати свій passport.

---

### 2. Native Authentication (сервіс інтегрований)

Це основний і найкращий flow. Працює як OAuth, але для агентів.

```
Крок 1: Агент приходить на сервіс
─────────────────────────────────
Agent → Service: POST /auth/agent
Headers: {
  X-Agent-Passport: "ap_7xk2m9f3",
  X-Agent-Signature: "<підпис challenge private key-єм>"
}

Крок 2: Сервіс верифікує через AgentPass
──────────────────────────────────────
Service → AgentPass API: POST /verify
Body: {
  passport_id: "ap_7xk2m9f3",
  signature: "...",
  challenge: "random-nonce-123"
}

AgentPass перевіряє:
  ✓ Passport існує і активний (не revoked)
  ✓ Підпис валідний (public key з passport)
  ✓ Owner верифікований
  ✓ Trust score ≥ мінімуму сервісу
  ✓ Агент не заблокований

AgentPass → Service: {
  verified: true,
  agent: {
    id: "ap_7xk2m9f3",
    name: "KDN Sales Bot",
    owner: "roman@kdn.agency",
    trust_score: 87
  }
}

Крок 3: Сервіс створює сесію
────────────────────────────
Service → Agent: {
  session_token: "sess_abc123",
  expires_in: 3600
}

Agent зберігає token у свій credential vault.
```

**Час: ~500ms. Без email, без пароля, без CAPTCHA.**

Для сервісу це виглядає як ще один OAuth provider:

```html
<!-- Поряд з існуючими кнопками -->
<button>Login with Google</button>
<button>Login with GitHub</button>
<button>Login with AgentPass</button>  <!-- Нове -->
```

---

### 3. Fallback Authentication (сервіс НЕ інтегрований)

Коли сервіс не підтримує AgentPass, агент реєструється як людина, використовуючи можливості свого passport.

```
Крок 1: Агент визначає що сервіс не інтегрований
────────────────────────────────────────────────
Agent → AgentPass: authenticate("github.com")
AgentPass: перевіряє /.well-known/agentpass.json на github.com
AgentPass → Agent: { native: false, fallback: true }

Крок 2: Агент перевіряє чи є збережені credentials
──────────────────────────────────────────────────
Agent → AgentPass: get_credentials("github.com")
AgentPass → Agent: null (ще не реєструвався)

Крок 3: Реєстрація через browser automation
──────────────────────────────────────────
Agent → AgentPass: register_human_mode("github.com/signup")

AgentPass під капотом:
  a) Відкриває Playwright browser session (через proxy якщо налаштовано)
  b) Navigates to github.com/signup
  c) Шукає email signup форму (ігнорує OAuth кнопки "Login with Google")
  d) Заповнює форму:
     - Email: kdn-sales-bot@agent-mail.xyz (з passport)
     - Password: <генерує secure password>
     - Username: <генерує>
  e) Submit

Крок 4: CAPTCHA (якщо є)
────────────────────────
AgentPass:
  a) Виявляє CAPTCHA на сторінці
  b) Робить screenshot
  c) Надсилає owner-у через webhook:
     "🤖 Агент натрапив на CAPTCHA при реєстрації на github.com"
     [Screenshot прикріплений]
     [Інтерактивне вікно для вирішення CAPTCHA / або лінк на browser session]
  d) Owner вирішує CAPTCHA через dashboard (бачить live browser session)
  e) Агент продовжує реєстрацію

Крок 5: Email verification
─────────────────────────
AgentPass:
  a) wait_for_email("kdn-sales-bot@agent-mail.xyz", subject: "GitHub")
  b) Лист приходить через webhook (Cloudflare Email Workers)
  c) extract_verification_link(email)
  d) navigate(verification_link)
  e) Аккаунт підтверджений ✓

Крок 6: Збереження credentials
─────────────────────────────
AgentPass → Credential Vault (локально): store({
  service: "github.com",
  username: "kdn-sales-bot-7x",
  password: "<encrypted>",
  email: "kdn-sales-bot@agent-mail.xyz",
  cookies: [...],
  registered_at: "2026-02-11"
})

Крок 7: Повторний логін (наступного разу)
───────────────────────────────────────
Agent → AgentPass: authenticate("github.com")
AgentPass: get_credentials → є!
AgentPass: open_browser → navigate to login → fill credentials → done
Agent має активну сесію за 5 секунд.
```

**Час першої реєстрації: ~30-60 секунд (без CAPTCHA), ~2-5 хвилин (з CAPTCHA і owner approval). Повторний логін: ~5 секунд.**

---

### 4. Session Management

Сесії протухають, cookies expire. AgentPass обробляє це автоматично:

```
Agent → AgentPass: authenticate("github.com")

AgentPass:
  1. Перевіряє credential vault → є збережена сесія
  2. Пробує використати збережені cookies/token
  3. Якщо працює → повертає активну сесію ✅
  4. Якщо 401/403 (сесія expired):
     a) Пробує re-login через збережені credentials (username + password)
     b) Якщо login працює → оновлює cookies в vault → ✅
     c) Якщо і credentials не працюють (пароль змінено, акаунт заблокований):
        → Повідомляє агента: { error: "auth_failed", reason: "credentials_invalid" }
        → Надсилає webhook owner-у: "⚠️ Не можу залогінитись на github.com"
        → Агент вирішує: спробувати інший підхід або зупинитись
```

**Принцип: спробував → не вийшло → спробував ще раз з credentials → не вийшло → повідомив owner-а. Жодних нескінченних retry.**

---

### 5. OAuth Handling Strategy

Багато сайтів пропонують "Login with Google" як основний спосіб. Стратегія агента:

```
AgentPass при fallback реєстрації:
  1. Відкриває сторінку реєстрації
  2. Шукає email/password форму (не OAuth кнопки)
  3. Якщо є email signup → використовує його
  4. Якщо ТІЛЬКИ OAuth (немає email signup):
     → Повідомляє агента: { error: "oauth_only", service: "example.com" }
     → Webhook owner-у: "⚠️ example.com підтримує тільки OAuth. Зареєструйтесь вручну."
     → Owner реєструється сам, потім вводить credentials в dashboard
     → AgentPass зберігає їх в vault агента
```

**Для MVP: агент завжди шукає email signup. OAuth — це проблема для майбутніх версій.**

---

### 6. SMS/Phone Verification

Деякі сервіси вимагають телефон:

```
Agent → AgentPass: need_phone_verification("twitter.com")

AgentPass:
  1. Перевіряє чи в passport є phone capability
  2. Якщо ні → provision нового номера через Twilio
  3. Сервіс надсилає SMS на цей номер
  4. Twilio webhook → AgentPass отримує SMS
  5. extract_otp(sms) → "482910"
  6. Вводить OTP на сторінці через browser
  7. Verification passed ✓
```

---

### 7. Error Recovery

Реєстрація або логін може зламатись на будь-якому кроці. Стратегія:

```
Принцип: агент НЕ ретрайить нескінченно. 
Максимум 2 спроби, потім — повідомити owner-а.

Помилка при реєстрації (крок 3 з 5):
  1. AgentPass фіксує на якому кроці зламалось
  2. Робить screenshot поточного стану browser
  3. Надсилає webhook owner-у:
     "❌ Реєстрація на github.com зупинилась на кроці 'Email verification'
      Причина: Verification email не прийшов протягом 60 секунд
      [Screenshot]
      [🔄 Retry] [✋ Skip] [👤 Do it manually]"
  4. Owner вибирає дію
  5. Якщо Retry → агент починає з початку (нові credentials)
  6. Якщо Skip → агент продовжує інші задачі
  7. Якщо Manual → owner реєструється сам, вводить credentials через dashboard

Помилка при логіні:
  1. Спроба з cookies → fail
  2. Спроба з credentials → fail
  3. Webhook owner-у:
     "⚠️ Не можу залогінитись на github.com. Пароль змінено?"
     [👤 Update credentials] [🗑 Delete and re-register]
```

---

### 8. Trust Score System

Trust score — це числовий показник надійності агента (0-100). Він впливає на те, як сервіси ставляться до агента.

**Як рахується:**

```
Base score:
  +30: owner verified (email confirmed)
  +20: owner has payment method
  +10: passport age > 7 days
  +10: passport age > 30 days

Activity score:
  +1 за кожні 10 успішних автентифікацій (max +20)
  
Penalty:
  -50: abuse report (spam, ToS violation)
  -100: owner blocked / passport revoked
```

**Як використовується сервісами:**

```javascript
// Сервіс сам вирішує свій мінімальний trust score
const MIN_TRUST = 50;

app.post('/auth/agent', async (req, res) => {
  const result = await agentpass.verify(req.passport);
  
  if (result.trust_score >= MIN_TRUST) {
    // Пускаємо без додаткової верифікації
    return createSession(result.agent);
  } else if (result.trust_score >= 20) {
    // Пускаємо, але з обмеженнями
    return createLimitedSession(result.agent);
  } else {
    // Відмовляємо
    return res.status(403).json({ error: "Trust score too low" });
  }
});
```

---

### 9. Permissions & Bounded Autonomy

Owner контролює що агент може робити без запиту:

```yaml
permissions:
  auto_approved:        # Агент робить сам
    - registration
    - login
    - read_data
    - browse_web
    
  requires_approval:    # Агент запитує owner-а
    - payments
    - delete_account  
    - send_email_external
    - share_data
    
  blocked:              # Заборонено повністю
    - banking_sites
    - government_sites
    - adult_content
```

**Approval flow:**

```
Agent хоче зробити покупку на Amazon:
1. Agent → AgentPass: action("purchase", "amazon.com", amount: 49.99)
2. AgentPass перевіряє permissions → requires_approval
3. AgentPass → Owner (webhook):
   "🤖 KDN Sales Bot хоче купити товар на Amazon за $49.99. Дозволити?"
4. Owner: "✅ Approve"
5. AgentPass → Agent: approved, proceed
6. Agent завершує покупку
```

---

### 10. Passport Revocation

Якщо passport скомпрометований або агент більше не потрібен:

```
Owner → AgentPass Dashboard: "Revoke passport ap_7xk2m9f3"

AgentPass:
  1. Помічає passport як revoked в API
  2. Будь-який сервіс який робить verify() отримує:
     { verified: false, reason: "passport_revoked" }
  3. Native auth перестає працювати миттєво
  4. Локальний credential vault:
     - Можна видалити (owner натискає "Wipe credentials")
     - Або залишити (credentials стають orphaned)
  5. Для fallback сервісів (де агент зареєструвався як людина):
     - Акаунти продовжують існувати (AgentPass не може їх видалити)
     - Але агент більше не може ними користуватись (private key видалено)
     - Owner може вручну видалити акаунти якщо потрібно
  6. Webhook owner-у: "✅ Passport ap_7xk2m9f3 revoked. 12 credentials orphaned."
```

---

### 11. Webhook Notifications

Owner отримує нотифікації про ВСЕ що робить агент (не тільки approvals):

```
Webhook events:

agent.registered        — агент зареєструвався на новому сервісі
agent.logged_in         — агент залогінився
agent.login_failed      — не вдалось залогінитись
agent.captcha_needed    — агент натрапив на CAPTCHA, потрібна допомога
agent.approval_needed   — агент хоче зробити sensitive дію
agent.email_received    — на поштову скриньку агента прийшов лист
agent.sms_received      — агент отримав SMS
agent.error             — помилка при виконанні дії
agent.credential_stored — нові credentials збережені
agent.session_expired   — сесія на сервісі протухла
agent.passport_revoked  — паспорт відкликано
```

**Канали доставки (MVP):**
- Webhook URL (будь-який endpoint)
- Telegram Bot (inline кнопки для approve/deny/solve CAPTCHA)

**Формат:**
```json
{
  "event": "agent.captcha_needed",
  "agent": {
    "passport_id": "ap_7xk2m9f3",
    "name": "KDN Sales Bot"
  },
  "data": {
    "service": "github.com",
    "captcha_type": "recaptcha_v2",
    "screenshot_url": "https://...",
    "browser_session_url": "https://dashboard.agentpass.space/session/abc123"
  },
  "timestamp": "2026-02-11T10:15:00Z",
  "actions": [
    { "type": "solve_captcha", "url": "https://dashboard.agentpass.space/captcha/abc123" },
    { "type": "skip", "callback": "https://api.agentpass.space/actions/skip/abc123" }
  ]
}
```

---

### 12. Proxy Support

Для запобігання банів при множинних реєстраціях:

```yaml
# agentpass.config.yaml
proxy:
  enabled: true
  type: "rotating"        # rotating | sticky | manual
  provider: "brightdata"  # або будь-який SOCKS5/HTTP proxy
  url: "socks5://user:pass@proxy.example.com:1080"
  
  # Або пул проксі
  pool:
    - "socks5://proxy1.example.com:1080"
    - "socks5://proxy2.example.com:1080"
    - "http://proxy3.example.com:8080"
  
  rules:
    - domain: "github.com"
      proxy: "sticky"      # Один IP для одного сервісу
    - domain: "*.google.com"
      proxy: "none"        # Без проксі
```

**Для MVP:** опціональний параметр `proxy_url` в конфігурації. Без складного пулу — просто один проксі на вибір.

---

## Архітектура системи

```
┌─────────────────────────────────────────────────────────┐
│                      CLIENTS                             │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ Claude   │  │ OpenClaw │  │ Any AI   │              │
│  │ Code     │  │          │  │ Agent    │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│       │              │              │                    │
│       └──────────────┼──────────────┘                    │
│                      │                                   │
│               MCP Protocol                               │
│                      │                                   │
├──────────────────────┼───────────────────────────────────┤
│                      ▼                                   │
│  ┌──────────────────────────────────────┐               │
│  │        AgentPass MCP Server          │               │
│  │        (runs locally)                │               │
│  │                                      │               │
│  │  Tools:                              │               │
│  │  - create_identity()                 │               │
│  │  - authenticate(url)                 │               │
│  │  - register_human_mode(url)          │               │
│  │  - get_credentials(service)          │               │
│  │  - list_identities()                 │               │
│  │  - request_approval(action)          │               │
│  └──────────────┬───────────────────────┘               │
│                 │                                        │
│        ┌────────┼────────┬────────────┐                 │
│        ▼        ▼        ▼            ▼                 │
│  ┌─────────┐┌────────┐┌────────┐┌──────────┐           │
│  │ Passport││ Email  ││Browser ││Credential│           │
│  │ Service ││Service ││Service ││  Vault   │           │
│  │         ││        ││        ││ (LOCAL)  │           │
│  │- create ││- create││- open  ││- store   │           │
│  │- sign   ││  inbox ││  page  ││- get     │           │
│  │- verify ││- recv  ││- fill  ││- list    │           │
│  │  (→API) ││- send  ││- click ││- delete  │           │
│  │- trust  ││- parse ││- read  ││- encrypt │           │
│  │  score  ││        ││- proxy ││          │           │
│  └────┬────┘└───┬────┘└───┬────┘└────┬─────┘           │
│       │         │         │          │                  │
├───────┼─────────┼─────────┼──────────┼──────────────────┤
│       │         │         │          │  INFRASTRUCTURE  │
│       ▼         ▼         ▼          ▼                  │
│  ┌─────────┐┌────────┐┌────────┐┌──────────┐           │
│  │AgentPass││Cloudfl.││Playwri-││ SQLite + │           │
│  │  API    ││ Email  ││  ght   ││ AES-256  │           │
│  │(public  ││Workers ││ (+opt  ││ (LOCAL)  │           │
│  │ keys,  ││        ││ proxy) ││          │           │
│  │ trust) ││        ││        ││          │           │
│  └─────────┘└────────┘└────────┘└──────────┘           │
│       │         │                                       │
│       │         ▼                                       │
│       │   ┌──────────┐                                  │
│       │   │  Twilio   │ (SMS/Phone)                     │
│       │   └──────────┘                                  │
│       │                                                  │
│       ▼ (future: on-chain)                              │
│  ┌──────────────┐                                       │
│  │  Base/ETH     │ (Web3 roadmap: public key registry)  │
│  │  Smart        │                                      │
│  │  Contract     │                                      │
│  └──────────────┘                                       │
│                                                          │
├─────────────────────────────────────────────────────────┤
│                 OWNER INTERFACE                           │
│                                                          │
│  ┌──────────────────────────────────────┐               │
│  │          Web Dashboard               │               │
│  │                                      │               │
│  │  - All agents and their passports    │               │
│  │  - Live activity feed (audit log)    │               │
│  │  - Credential vault overview         │               │
│  │  - Pending approval requests         │               │
│  │  - CAPTCHA solving interface         │               │
│  │  - Live browser sessions (view)      │               │
│  │  - Trust score history               │               │
│  │  - Email inboxes                     │               │
│  │  - Webhook configuration             │               │
│  │  - Passport revocation               │               │
│  └──────────────────────────────────────┘               │
│                                                          │
│  ┌──────────────────────────────────────┐               │
│  │        Telegram Bot                  │               │
│  │                                      │               │
│  │  - Approval requests (inline btns)   │               │
│  │  - CAPTCHA screenshots               │               │
│  │  - Error notifications               │               │
│  │  - Activity digest                   │               │
│  └──────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────┘
```

---

## Інтеграція з сервісами (SDK)

### Для сервісу який хоче підтримати AgentPass

**1. Well-known endpoint:**

Сервіс додає файл `/.well-known/agentpass.json`:

```json
{
  "agentpass_supported": true,
  "version": "1.0",
  "auth_endpoint": "/api/auth/agent",
  "min_trust_score": 50,
  "capabilities_required": ["email"],
  "agent_limits": {
    "rate_limit": "100/hour",
    "max_agents_per_owner": 5
  }
}
```

**2. Backend SDK (Node.js):**

```javascript
import { AgentPass } from '@agentpass/sdk';

const agentpass = new AgentPass({
  serviceId: 'srv_myapp',
  apiKey: process.env.AGENTPASS_API_KEY
});

// Middleware
app.post('/api/auth/agent', async (req, res) => {
  const { passport_id, signature, challenge } = req.body;
  
  const result = await agentpass.verify({ passport_id, signature, challenge });
  
  if (!result.verified) {
    return res.status(401).json({ error: result.reason });
  }
  
  // Створити або знайти користувача-агента
  let user = await db.findByAgentId(result.agent.id);
  if (!user) {
    user = await db.createAgentUser({
      agent_id: result.agent.id,
      agent_name: result.agent.name,
      owner_email: result.agent.owner,
      trust_score: result.agent.trust_score
    });
  }
  
  const token = generateSessionToken(user);
  res.json({ session_token: token, user_id: user.id });
});
```

**3. Frontend widget:**

```html
<script src="https://api.agentpass.space/widget.js"></script>

<!-- Автоматично з'являється якщо сторінку відкрив агент -->
<!-- Для людей — не показується -->
```

---

## MCP Server — повний список тулів

Це те, що AI агент безпосередньо викликає:

### Identity Management

| Tool | Опис |
|------|------|
| `create_identity(name, capabilities)` | Створити нового агента з passport |
| `list_identities()` | Показати всіх агентів owner-а |
| `get_identity(passport_id)` | Деталі конкретного агента |
| `delete_identity(passport_id)` | Видалити агента і всі його credentials |
| `revoke_identity(passport_id)` | Відкликати паспорт (миттєво, без видалення даних) |

### Authentication

| Tool | Опис |
|------|------|
| `authenticate(service_url)` | Авторизуватись на сервісі (auto-detect native/fallback) |
| `register(service_url, options)` | Зареєструватись на сервісі |
| `logout(service_url)` | Вийти з сервісу |
| `get_session(service_url)` | Отримати активну сесію/токен |

### Email

| Tool | Опис |
|------|------|
| `get_email_address()` | Отримати email адресу агента |
| `wait_for_email(filter, timeout)` | Чекати на лист |
| `read_email(email_id)` | Прочитати лист |
| `extract_verification_link(email_id)` | Витягти посилання підтвердження |
| `extract_otp_code(email_id)` | Витягти OTP код з листа |
| `send_email(to, subject, body)` | Надіслати лист |
| `list_emails(filter)` | Список листів |

### Phone/SMS

| Tool | Опис |
|------|------|
| `get_phone_number()` | Отримати номер телефону агента |
| `wait_for_sms(timeout)` | Чекати на SMS |
| `extract_otp_from_sms(sms_id)` | Витягти OTP з SMS |

### Browser

| Tool | Опис |
|------|------|
| `open_browser(url)` | Відкрити сторінку (з proxy якщо налаштовано) |
| `fill_form(fields)` | Заповнити форму |
| `click(selector_or_text)` | Клікнути |
| `get_page_content()` | Отримати текст сторінки |
| `screenshot()` | Зробити скріншот |
| `wait_for_element(selector, timeout)` | Чекати на елемент |

### Credentials

| Tool | Опис |
|------|------|
| `store_credential(service, data)` | Зберегти credentials |
| `get_credential(service)` | Отримати credentials |
| `list_credentials()` | Всі збережені сервіси |
| `delete_credential(service)` | Видалити credentials |

### Approvals & Notifications

| Tool | Опис |
|------|------|
| `request_approval(action, details)` | Запросити дозвіл у owner-а |
| `check_approval(request_id)` | Перевірити статус запиту |
| `notify_owner(event, details)` | Надіслати нотифікацію owner-у |
| `escalate_to_owner(problem, screenshot)` | Ескалація проблеми (CAPTCHA, помилка) |

---

## Сценарії використання

### Сценарій 1: Агент налаштовує робоче середовище

```
Owner: "Зареєструйся на GitHub, Linear і Vercel. Створи проєкт 'my-startup'."

Agent:
1. authenticate("github.com") → fallback → реєстрація → ✅
2. Створює repo "my-startup" на GitHub
3. authenticate("linear.app") → fallback → реєстрація → ✅  
4. Створює проєкт "My Startup" в Linear
5. authenticate("vercel.com") → fallback → реєстрація → ✅
6. Зв'язує Vercel з GitHub repo
7. Все готово. Credentials збережені. Наступного разу — логін за 5 сек.
```

### Сценарій 2: Агент моніторить email і реагує

```
Owner: "Слідкуй за листами від клієнтів і створюй тікети в Linear."

Agent (в loop):
1. wait_for_email(from: "*@client.com")
2. Лист прийшов: "У нас баг з оплатою"
3. authenticate("linear.app") → є credentials → login → ✅
4. Створює тікет: "Bug: payment issue reported by client"
5. send_email(to: client, body: "Дякую, створив тікет LIN-42")
6. Повертається до кроку 1
```

### Сценарій 3: Агент з native auth

```
Сервіс "analytics-tool.com" інтегрував AgentPass SDK.

Agent: authenticate("analytics-tool.com")
AgentPass: checks /.well-known/agentpass.json → native supported!
AgentPass: sends passport + signature → verified → session token
Agent: має повний доступ за 500ms, без реєстрації
```

### Сценарій 4: Approval flow для sensitive дій

```
Agent знайшов ідеальний домен на Namecheap за $12.

Agent → AgentPass: request_approval("purchase", {
  service: "namecheap.com",
  action: "buy domain my-startup.com",
  amount: "$12.00"
})

Owner отримує в Telegram:
"🤖 KDN Sales Bot хоче купити домен my-startup.com за $12.00 на Namecheap. 
 [✅ Approve] [❌ Deny]"

Owner натискає Approve.

Agent продовжує покупку.
```

### Сценарій 5: CAPTCHA ескалація

```
Agent реєструється на twitter.com, натрапив на reCAPTCHA.

Agent → AgentPass: escalate_to_owner("captcha", screenshot)

Owner отримує в Telegram:
"🤖 KDN Sales Bot натрапив на CAPTCHA при реєстрації на twitter.com
 [📸 Screenshot]
 [🖥 Відкрити browser session] [⏭ Skip]"

Owner відкриває browser session в dashboard → бачить живу сторінку →
вирішує CAPTCHA → AgentPass детектить що CAPTCHA пройдена → агент продовжує.
```

### Сценарій 6: Помилка і recovery

```
Agent реєструється на notion.com. Email verification не прийшов.

AgentPass чекає 60 секунд → timeout.

Webhook → Owner:
"❌ Реєстрація на notion.com зупинилась.
 Причина: Verification email не прийшов (timeout 60s)
 [🔄 Retry] [✋ Skip] [👤 Зроблю вручну]"

Owner натискає Retry.
Agent починає реєстрацію заново з новим username.
Цього разу email приходить → ✅
```

---

## Audit Log

Кожна дія агента логується:

```json
{
  "audit_log": [
    {
      "timestamp": "2026-02-11T10:15:00Z",
      "agent": "ap_7xk2m9f3",
      "action": "register",
      "service": "github.com",
      "method": "fallback_human_mode",
      "result": "success",
      "duration_ms": 34500,
      "details": {
        "email_used": "kdn-sales-bot@agent-mail.xyz",
        "username_created": "kdn-sales-bot-7x"
      }
    },
    {
      "timestamp": "2026-02-11T10:16:00Z",
      "agent": "ap_7xk2m9f3",
      "action": "authenticate",
      "service": "linear.app",
      "method": "native_agentpass",
      "result": "success",
      "duration_ms": 420
    },
    {
      "timestamp": "2026-02-11T10:20:00Z",
      "agent": "ap_7xk2m9f3",
      "action": "captcha_escalation",
      "service": "twitter.com",
      "result": "resolved_by_owner",
      "duration_ms": 125000,
      "details": {
        "captcha_type": "recaptcha_v2",
        "owner_response_time_ms": 120000
      }
    },
    {
      "timestamp": "2026-02-11T11:00:00Z",
      "agent": "ap_7xk2m9f3",
      "action": "purchase",
      "service": "amazon.com",
      "method": "fallback_human_mode",
      "result": "pending_approval",
      "approval": {
        "requested_at": "2026-02-11T11:00:00Z",
        "approved_at": "2026-02-11T11:02:15Z",
        "approved_by": "owner_r4k2m"
      }
    }
  ]
}
```

Owner бачить це все у Dashboard в реальному часі.

---

## Безпека (Зведена)

### Cryptographic Identity
- Кожен passport має пару Ed25519 ключів
- Private key зберігається ТІЛЬКИ локально на машині агента
- Public key реєструється на AgentPass API для verify
- Автентифікація — challenge-response з підписом
- Неможливо підробити passport без private key

### Credential Vault Encryption
- AES-256-GCM для шифрування credentials at rest
- Master key — derive від passport private key
- Credentials ніколи не передаються в plaintext
- Vault живе ТІЛЬКИ локально

### Bounded Autonomy
- Owner задає дозволені/заборонені дії
- Sensitive дії вимагають explicit approval
- Blocked domains — агент фізично не може туди зайти
- CAPTCHA → ескалація до owner-а

### Isolation
- Кожен агент = окремий passport, окремі credentials
- Компрометація одного агента не впливає на інших
- Credentials не шаряться між агентами

### Audit Trail
- Кожна дія логується з timestamp, деталями, результатом
- Логи immutable (append-only)
- Owner має повний доступ через dashboard
- Webhook нотифікації для real-time моніторингу

### Passport Revocation
- Owner може миттєво відкликати passport
- Native auth перестає працювати одразу
- Credential vault може бути wiped

### Abuse Prevention
- Trust score падає при abuse reports
- Rate limiting на реєстрації (max N per day)
- Owner verification обов'язкова
- Сервіси можуть задавати мінімальний trust score
- Proxy support для уникнення IP-банів

### Error Handling
- Max 2 retry на будь-яку дію
- Ескалація до owner-а при невдачі
- Ніколи не ретрайить нескінченно

---

## Tech Stack

| Компонент | Технологія |
|-----------|-----------|
| MCP Server | TypeScript, @modelcontextprotocol/sdk |
| API Server | Hono (lightweight, edge-ready) |
| Database (API) | SQLite (dev) / Turso (prod) |
| Database (local) | SQLite (credential vault) |
| Email | Cloudflare Email Workers + Email Routing |
| SMS | Twilio Programmable SMS |
| Browser | Playwright |
| Proxy | SOCKS5/HTTP proxy (configurable) |
| Crypto | Node.js crypto (Ed25519, AES-256-GCM) |
| Dashboard | React + Tailwind (або simple HTML) |
| Telegram Bot | grammY |
| Packaging | npm package (`npx agentpass`) + Docker |

---

## Deployment (Self-hosted for MVP)

AgentPass MVP біжить локально на машині де працюють агенти:

```bash
# Встановлення
npm install -g @agentpass/cli

# Ініціалізація (створює owner акаунт, конфігурацію)
agentpass init

# Запуск MCP server + dashboard
agentpass serve

# Dashboard доступний на http://localhost:3847
# MCP server доступний для Claude Code через stdio/SSE
```

```yaml
# ~/.agentpass/config.yaml
owner:
  email: "roman@kdn.agency"

server:
  port: 3847
  
email:
  domain: "agent-mail.xyz"  # або свій домен
  provider: "cloudflare"

sms:
  provider: "twilio"
  account_sid: "..."
  auth_token: "..."

proxy:
  enabled: false
  url: ""

webhooks:
  - url: "https://api.telegram.org/bot.../sendMessage"
    events: ["*"]

# Claude Code MCP config (додати в ~/.claude/mcp.json)
# {
#   "mcpServers": {
#     "agentpass": {
#       "command": "agentpass",
#       "args": ["mcp"]
#     }
#   }
# }
```

---

## Що робимо для хакатону (MVP scope)

### Must have (День 1-2):
- [ ] Agent Passport — create, verify, sign (Ed25519)
- [ ] MCP Server з основними тулами
- [ ] Email service (Cloudflare) — create mailbox, receive, parse verification links
- [ ] Browser service (Playwright) — navigate, fill, click, screenshot
- [ ] Credential vault (SQLite + AES-256, локально)
- [ ] Fallback auth flow (end-to-end: register on a real site)
- [ ] Session management (auto re-login on expired session)

### Should have (День 3):
- [ ] Native auth flow (demo site з SDK)
- [ ] Trust score (basic calculation)
- [ ] Web dashboard (agent list, audit log, live emails)
- [ ] CAPTCHA escalation (screenshot → owner)
- [ ] Webhook notifications
- [ ] Error recovery flow
- [ ] SMS verification (Twilio)

### Nice to have (День 4+):
- [ ] Telegram bot for owner
- [ ] Owner approval flow (approve/deny через Telegram)
- [ ] Live browser session viewing in dashboard
- [ ] Well-known endpoint discovery
- [ ] Service SDK (npm package)
- [ ] Proxy support configuration
- [ ] Passport revocation
- [ ] Demo video

---

## Vision / Market Opportunity

### Чому зараз

Authentication визнаний bottleneck #1 в AI agent ecosystem. Mercedes Bent (інвестор, VC) провела івент в SF на тему "що missing для агентів" — висновок: **"Authentication is the missing foundational layer."** OpenAI запустив Frontier — платформу для управління агентами як "цифровими працівниками". Palo Alto Networks прогнозує 82 агенти на 1 людину до кінця 2026. Всі будують агентів, але ніхто не побудував для них паспортний стіл.

### Масштаб можливостей

**1. Agent Commerce — агенти як покупці**

Маркетплейс інтегрує AgentPass → агенти заходять, обирають товари, купують. Маркетплейс бачить: trust score 90, owner верифікований, платіжний метод є → instant checkout без жодної людини. Це новий канал продажів. Amazon, Shopify, будь-який e-commerce отримує мільйони "клієнтів-агентів".

**2. Agent-to-Agent Economy — агенти торгують між собою**

Агент-А надає сервіс (генерація лого, переклад, аналітика). Агент-Б хоче замовити. Обидва авторизуються через AgentPass → перевіряють trust score один одного → транзакція автоматична. Повністю автономна економіка агентів без участі людей.

**3. B2B Procurement Automation**

Агент компанії А заходить на сайт компанії Б через AgentPass → бачить каталог → формує замовлення → owner компанії А отримує approval в Telegram → підтверджує → замовлення оформлене. Весь B2B procurement без менеджерів, дзвінків, email-переписок.

**4. SaaS Auto-Onboarding — агент як менеджер**

SaaS ставить "Login with AgentPass" → агент сам реєструється, обирає план, підключає інтеграції, налаштовує проєкт, імпортує дані. Owner приходить на повністю готове робоче середовище. Час onboarding: з 2 годин до 5 хвилин.

**5. Developer Infrastructure — DevOps без DevOps**

CI/CD агент з одним passport: реєструється на npm → публікує пакети → створює issues на GitHub → деплоїть на Vercel → моніторить Sentry → фіксить баги → створює PR. Повний development lifecycle з одним passport.

**6. Freelance & Hiring Automation**

Owner каже агенту: "знайди фрілансера на Upwork для дизайну лендінга, бюджет $500". Агент реєструється на Upwork → постить замовлення → фільтрує відповіді → шортлістить топ-3 → показує owner-у. Owner тільки обирає.

**7. Research & Data Collection**

Агент реєструється на 20 різних data sources → збирає інформацію → агрегує → генерує звіт. Маркетинговий ресьорч, competitive analysis, lead generation — все автоматично.

### Мережевий ефект

AgentPass працює як Visa/Mastercard для AI агентів:

```
Більше агентів з AgentPass
        ↓
Більше сервісів інтегрують SDK (бо хочуть цих "клієнтів")
        ↓
Більше можливостей для агентів
        ↓
Більше людей створюють агентів з AgentPass
        ↓
(цикл повторюється)
```

Кожен сервіс який інтегрує AgentPass отримує доступ до всіх агентів мережі. Кожен агент з AgentPass отримує доступ до всіх інтегрованих сервісів. Winner-takes-all динаміка.

### Бізнес-модель (post-hackathon vision)

```
Free tier:
  - 1 агент
  - 5 реєстрацій/день
  - Email тільки
  - Community trust score

Pro ($29/mo):
  - 10 агентів
  - Unlimited реєстрації
  - Email + SMS + Browser
  - Priority trust score
  - Telegram bot notifications
  - Dashboard analytics

Enterprise (custom):
  - Unlimited агентів
  - Custom domain для email
  - SLA на API uptime
  - Dedicated support
  - On-premise option
  - Custom trust score policies

Service SDK:
  - Free для інтеграції
  - Revenue share на premium features (agent analytics, fraud detection)
```

### Конкуренти

Прямих конкурентів **немає**. Є суміжні рішення:

| Рішення | Що робить | Чого не вистачає |
|---------|-----------|-----------------|
| Auth0 / Clerk | Auth для людей | Не працює для агентів |
| Arcade.dev | Tool auth для агентів | Тільки API tokens, не identity |
| BrowserBase | Headless browsers | Без identity, без email, без trust |
| Operator (OpenAI) | Browser agent | Closed ecosystem, не протокол |
| Anon.com | Auth для агентів | Тільки credential forwarding, не identity |

AgentPass — єдиний повний identity layer: passport + email + browser + credentials + trust + SDK для сервісів.

---

## Назва і позиціонування

**AgentPass** — The Identity Layer for Autonomous AI Agents

Tagline: *"Give your AI agents a passport to the internet."*

Positioning: **"Auth0 for AI Agents"** — одна фраза, суддям одразу зрозуміло.

`Login with AgentPass` — як "Login with Google", тільки для агентів.