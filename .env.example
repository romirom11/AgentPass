# ========================================
# AgentPass Environment Variables
# ========================================
# This file contains ALL environment variables used across the AgentPass project.
# Copy this file to .env and fill in the values for your environment.

# ========================================
# API Server (@agentpass/api-server)
# ========================================

# Node environment (development, production, test)
NODE_ENV=development

# API server port
AGENTPASS_PORT=3846

# Database file path (SQLite/libSQL)
# For local development: ./agentpass.db
# For production (Docker): /app/data/agentpass.db
# For Turso (production): libsql://your-database.turso.io
AGENTPASS_DB_PATH=./agentpass.db

# Database auth token (required for Turso, optional for local SQLite)
# AGENTPASS_DB_TOKEN=your-turso-auth-token

# Webhook secret for email notifications
# Must match the secret configured in Cloudflare Worker
# Generate with: openssl rand -base64 32
WEBHOOK_SECRET=your-webhook-secret-here

# ========================================
# Dashboard (@agentpass/dashboard)
# ========================================

# API Server URL (used by the dashboard frontend)
# For local development: http://localhost:3846
# For production: https://api.agentpass.space
VITE_API_URL=http://localhost:3846

# ========================================
# Landing Page (@agentpass/landing)
# ========================================

# API Server URL (used by the landing page)
VITE_API_URL=http://localhost:3846

# ========================================
# Email Service (@agentpass/email-service)
# ========================================
# Note: Email service runs on Cloudflare Workers, configured via wrangler.toml
# These variables are set via wrangler secret put, not .env

# API_SERVER_URL - set in wrangler.toml vars section
#   Development: http://localhost:3846
#   Production: https://api.agentpass.space

# WEBHOOK_SECRET - set via: wrangler secret put WEBHOOK_SECRET
#   Must match WEBHOOK_SECRET in API server

# ========================================
# Browser Service (@agentpass/browser-service)
# ========================================

# Proxy configuration (optional, for fallback mode)
# PROXY_TYPE=socks5
# PROXY_HOST=127.0.0.1
# PROXY_PORT=1080
# PROXY_USERNAME=
# PROXY_PASSWORD=

# ========================================
# MCP Server (@agentpass/mcp-server)
# ========================================

# MCP server runs locally and uses the local credential vault
# No environment variables required

# API Server URL (where to register passports)
MCP_API_URL=http://localhost:3846

# Email Worker URL (where to fetch emails)
MCP_EMAIL_WORKER_URL=https://agentpass-email-worker-production.kudin-private.workers.dev

# ========================================
# SMS Service (Twilio)
# ========================================
# Note: SMS integration is not yet implemented
# These are placeholder variables for future use

# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+15550001234

# ========================================
# Production URLs
# ========================================
# Landing Page: https://agentpass.space
# API Server: https://api.agentpass.space
# Dashboard: https://dashboard.agentpass.space
# Email Worker: https://email.agentpass.space
# Email Domain: @agent-mail.xyz
